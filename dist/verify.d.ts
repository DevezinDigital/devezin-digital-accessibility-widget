/**
 * verify.ts — Entry for `@devezindigital/accessibility/verify`.
 *
 * Re-exports the checklist runner. Kept as a root file so the build emits a
 * flat `dist/verify.d.ts` matching the package `exports` map.
 */
export * from "./verify/checklist";
